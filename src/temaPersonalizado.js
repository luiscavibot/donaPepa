import { createMuiTheme } from '@material-ui/core/styles';
const temaPersonalizado = createMuiTheme({
    typography:{
      fontFamily: '"Raleway", sans-serif;',
      fontSize: 15,
    },
  });

export default temaPersonalizado;
  